services:
  # Web service voor de multiplayer game server
  - type: web
    name: muziek-raad-spel
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run server
    envVars:
      - key: NODE_ENV
        value: production
